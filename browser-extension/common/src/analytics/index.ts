// We will be refering to all the analytic functions as events
// It will be accessed in mv2 code as RQ.Events.Constants/Utils
import { EVENT_CONSTANTS } from "./eventContants";
import * as Utils from "./eventUtils";

export { Utils, EVENT_CONSTANTS as Constants };
