// TODO: @wrongsahil - Complete this for autogeneration of theme css file

// import { generateTheme } from "./ThemeProvider";
// import fs from "fs";

// const generateFile = (primaryColor: string, neutralColor: string) => {
//   const { theme, themeCssVariables } = generateTheme(primaryColor, neutralColor);
//   console.log({ theme, themeCssVariables });

//   fs.writeFileSync(
//     "theme.css",
//     `
//     :root {
//       ${Object.entries(themeCssVariables)
//         .map(([key, value]) => `${key}: ${value};`)
//         .join("\n")}
//     }`
//   );
//   console.log("theme.css file generated successfully");
// };

// generateFile("#004EEB", "#787878");
